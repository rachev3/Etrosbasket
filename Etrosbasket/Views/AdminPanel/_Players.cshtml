@model List<Etrosbasket.Models.Player>

<h3>Players List</h3>
<!-- Button to open the Create Player modal -->
<button class="btn btn-success mb-3" id="createPlayerBtn">Create New Player</button>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Born Year</th>
            <th>Weight (kg)</th>
            <th>Height (cm)</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var player in Model)
        {
            <tr>
                <td>@player.Name</td>
                <td>@player.BornYear</td>
                <td>@player.Weight</td>
                <td>@player.Height</td>
                <td>
                    <!-- View Details button -->
                    <button class="btn btn-info btn-sm details-player" data-player-id="@player.PlayerId">Details</button>

                    <!-- Edit button -->
                    <button class="btn btn-primary btn-sm edit-player" data-id="">Edit</button>

                    <!-- Delete button -->
                    <a class="btn btn-danger btn-sm delete-player" data-player-id="@player.PlayerId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<!-- Player Details Modal Structure -->
<div class="modal fade" id="playerDetailsModal" tabindex="-1" aria-labelledby="playerDetailsLabel" aria-hidden="true">
    <div class="modal-dialog modal-xl">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="playerDetailsLabel">Player Details</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body" id="modalContent" style="max-height: 75vh; overflow-y: auto;">
                <!-- The statistics table will load here dynamically -->
            </div>
        </div>
    </div>
</div>






